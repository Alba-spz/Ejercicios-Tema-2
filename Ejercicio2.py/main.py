# Se importa la función ejecutar desde el módulo lanzador
from lanzador import ejecutar

# Se verifica si el archivo se está ejecutando como programa principal
if __name__ == "__main__":
    # Se llama a la función ejecutar
    ejecutar()
