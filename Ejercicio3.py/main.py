# Importamos la función 'ejecutar' desde el módulo 'lanzador'
from lanzador import ejecutar

# Verificamos si el archivo se está ejecutando como programa principal
if __name__ == "__main__":
    # Llamamos a la función 'ejecutar'
    ejecutar()
